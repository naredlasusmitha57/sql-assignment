1-SELECT *
FROM WORKER_DETAILS
WHERE SALARY IN(
             SELECT SALARY
FROM WORKER_DETAILS
WHERE WORKER_ID
NOT IN
(
SELECT
MAX(WORKER_ID)
FROM(WORKER_DETAILS)
GROUP BY SALARY));


2-SELECT WORKER_ID,DEPARTMENT,FIRST_NAME,LAST_NAME
FROM WORKER_DETAILS
WHERE SALARY IN(
              SELECT MAX(SALARY)
 FROM WORKER_DETAILS
 GROUP BY DEPARTMENT);