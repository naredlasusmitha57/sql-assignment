1.  SELECT DISTINCT JOB
    FROM EMP;

2.  SELECT ENAME,JOB
    FROM EMP
    WHERE MGR IS NULL;

3.  SELECT E.ENAME,E.JOB
    FROM EMP E,DEPT D
    WHERE D.LOC='NEWYORK' AND COMM>'1000';
 
4. SELECT COUNT(ENAME)
   FROM EMP
   WHERE DEPTNO='30'; 

5. SELECT ENAME
   FROM EMP
   WHERE DEPTNO='30'; 

6. SELECT E.ENAME,D.LOC
   FROM EMP E
   INNER JOIN DEPT D
   WHERE E.DEPTN0


7. SELECT DISTINT ENAME
   FROM EMP
   JOIN
   SELECT DISTINT DNAME
   FROM DEPT

8. SELECT DISTINCT E.EMPNO
    FROM EMP E
    INNER JOIN EMP M
    ON E.MGR=M.EMPNO
    AND E.HIREDATE<M.HIREDATE;


9.  SELECT COUNT(ENAME)
    FROM EMP
    WHERE JOB='MANAGER';


10. SELECT ENAME,JOB
    FROM EMP
    WHERE MGR IS NULL;

11. SELECT ENAME,DEPTNO
    FROM EMP
    WHERE ENAME='SMITH';

12. SELECT E.ENAME,D.LOC
    FROM EMP E,DEPT D
    WHERE D.LOC IS NOT BOSTON;

13. SELECT E.ENAME,D.LOC
    FROM EMP E,DEPT D
    WHERE E.DEPTNO=D.DEPTNO;

14. SELECT ENAME
    FROM EMP
    WHERE SAL < (SELECT SAL
                 FROM EMP
                 WHERE ENAME='MILLER');

15. SELECT *
    FROM EMP
    WHERE DEPTNO='30';

16. SELECT ENAME
    FROM EMP
    WHERE SAL<(SELECT MIN(SAL)
    FROM EMP
    WHERE JOB='MANAGER');

17. SELECT ENAME,DEPTNO
    FROM EMP
    WHERE JOB='ACCOUNTING';

18. SELECT ENAME
    FROM EMP
    WHERE JOB='MANAGER';

19. SELECT JOB,SAL,
    CASE
    WHEN JOB != 'SALES'
    THEN 'NONSALES'
    WHEN SAL>1000 
    THEN 1000
    ELSE 'NULL'
    END 
    FROM EMP;   



20. SELECT SAL  AS LOW
    FROM EMP
    WHERE SAL<(SELECT SAL
            FROM EMP
			WHERE SAL=1000) 
    UNION
    SELECT SAL AS MEDIUM 
    FROM EMP
    WHERE SAL=(SELECT SAL
          FROM EMP
		  WHERE SAL BETWEEN 1000 AND 2500) 
    UNION
    SELECT SAL AS HIGH 
    FROM EMP
    WHERE SAL>(SELECT SAL
           FROM EMP
		   WHERE SAL=2500);
    